[project]
name = "taskcontroller"
version = "0.1.0"
description = "work 함수를 등록하고 체인으로 실행하는 태스크 컨트롤러"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["taskcontroller*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
